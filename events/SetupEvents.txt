add_namespace = setup_event

country_event = {
	id = setup_event.1
	hidden = yes
	
	fire_only_once = yes
	
	immediate = {
		every_country = {
			country_event = { id = setup_event.2 }
		}
	}
}

#Relationcheck for every country
country_event = {
	id = setup_event.2
	hidden = yes
	
	is_triggered_only = yes
	
	immediate = {
		set_country_flag = setup_event_1_fired
		if = {
			limit = { 
				has_idea = idea_eu_member 
			}
			country_event = european_union.1
		}
		if = {
			limit = {
				OR = {
					has_country_flag = sunni_islam
					has_country_flag = shia_islam
				}
			}
			country_event = islam.1
		}
	}
}